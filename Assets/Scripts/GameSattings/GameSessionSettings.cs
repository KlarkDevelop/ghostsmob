using UnityEngine;

[CreateAssetMenu(fileName = "Session Settings", menuName = "Session Settings")]
public class GameSessionSettings : ScriptableObject
{
    public float tempSpeed;
    public float madnessSpeed;
}
